environments:
  dev:
    commands:
    - name: print_dev
      command: echo 'in dev!' 2 3 4
    clusterProvisioner: localdev
  prod:
    clusterProvisioner: external
    clusterProvisionerArgs:
      registry: sanic.io
      masters: sanic.io
      kubeconfig: ~/.kube/sanic.io.config
deploy:
  folder: "deploy"
  templaterImage: "sanic/templater-golang:latest"